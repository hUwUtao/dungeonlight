import * as PIXI from "pixi.js";

export default class TiledMap extends PIXI.Container {
  constructor(resourceId: PIXI.LoaderResource);
}
declare const middleware: PIXI.ILoaderMiddleware;
export { middleware };
